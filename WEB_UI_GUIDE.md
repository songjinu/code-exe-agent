# 🌐 Web UI 사용 가이드

CodeEx Agent의 웹 인터페이스를 사용하여 브라우저에서 직접 AI 코드 생성 및 실행을 경험하세요!

## 🚀 빠른 시작

### 1. 서버 실행

#### Windows
```bash
start_web.bat
```

#### Linux/Mac
```bash
./start_web.sh
```

또는 직접:
```bash
python web_ui.py
```

### 2. 브라우저 열기

서버가 시작되면 자동으로 다음 주소에서 접근 가능합니다:

```
http://localhost:8000
```

## 🎨 화면 구성

### 메인 화면

```
┌─────────────────────────────────────────────────────────┐
│  🚀 CodeEx Agent                                        │
│  AI가 자동으로 코드를 생성하고 실행합니다                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  [질문 입력창]                              [실행 🎯]   │
├─────────────────────────────────────────────────────────┤
│  💡 예제 질문                                           │
│  ⭕ Create a Salesforce account...                     │
│  ⭕ Search for documents...                            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ✅ 생성된 코드                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ result = agent.execute(...)                       │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ✅ 실행 결과                                           │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Account created: {...}                            │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 📝 사용 방법

### 1. 질문 입력

질문 입력창에 자연어로 질문을 입력합니다:

**예제:**
- `Create a Salesforce account named 'ACME Corp'`
- `Search for all documents in Google Drive`
- `Update spreadsheet cell A1 with 'Hello World'`
- `Create a Salesforce account and then create an opportunity`

### 2. 실행

- **Enter 키** 또는 **실행 버튼** 클릭
- AI가 자동으로 코드를 생성하고 실행합니다
- 로딩 애니메이션이 표시됩니다

### 3. 결과 확인

#### 생성된 코드 섹션
- **설명**: 코드가 무엇을 하는지 설명
- **코드**: 실제 생성된 Python 코드
- **사용된 도구**: 어떤 MCP 도구를 사용했는지
- **상세 설명**: 단계별 설명

#### 실행 결과 섹션
- **출력**: 코드 실행 결과
- **반환값**: 함수가 반환한 값 (있는 경우)
- **오류**: 실행 중 발생한 오류 (있는 경우)

## 🎯 예제 시나리오

### 시나리오 1: Salesforce 계정 생성

**질문:**
```
Create a Salesforce account named 'Tech Solutions Inc' in the Technology industry
```

**결과:**
```python
# 생성된 코드
result = agent.execute(
    'salesforce',
    'accounts',
    'create',
    {
        'name': 'Tech Solutions Inc',
        'industry': 'Technology'
    }
)
print(f"Account created: {result}")

# 출력
Account created: {'id': 'acc_123', 'name': 'Tech Solutions Inc'}
```

### 시나리오 2: 문서 검색

**질문:**
```
Search for all documents in Google Drive that contain 'quarterly report'
```

**결과:**
```python
# 생성된 코드
results = agent.execute(
    'google-drive',
    'search',
    'by_name',
    {'query': 'quarterly report'}
)
print(f"Found {len(results)} documents")
for doc in results:
    print(f"- {doc['name']}")

# 출력
Found 3 documents
- Q1 Quarterly Report
- Q2 Quarterly Report
- Annual Quarterly Summary
```

### 시나리오 3: 복합 작업

**질문:**
```
Create a new Salesforce account named 'Mega Corp' and then create a related opportunity worth $500,000
```

**결과:**
```python
# 생성된 코드
# Step 1: 계정 생성
account = agent.execute(
    'salesforce',
    'accounts',
    'create',
    {'name': 'Mega Corp'}
)

# Step 2: 기회 생성
opportunity = agent.execute(
    'salesforce',
    'opportunities',
    'create',
    {
        'account_id': account['id'],
        'amount': 500000,
        'stage': 'Prospecting'
    }
)

print(f"Created account {account['id']} with opportunity {opportunity['id']}")

# 출력
Created account acc_456 with opportunity opp_789
```

## ⚙️ API 엔드포인트

Web UI는 다음 API를 제공합니다:

### POST /api/query
질문을 처리하고 코드를 생성/실행합니다.

**요청:**
```json
{
  "query": "Create a Salesforce account",
  "execute": true
}
```

**응답:**
```json
{
  "success": true,
  "query": "Create a Salesforce account",
  "generated_code": {
    "code": "...",
    "description": "...",
    "required_tools": [...],
    "explanation": "..."
  },
  "execution_result": {
    "success": true,
    "output": "...",
    "return_value": {...}
  }
}
```

### GET /api/health
시스템 상태를 확인합니다.

**응답:**
```json
{
  "status": "ok",
  "workflow_ready": true,
  "api_key_set": true,
  "mcp_structure": true
}
```

## 🔧 고급 사용법

### curl로 API 호출

```bash
curl -X POST http://localhost:8000/api/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Create a Salesforce account named ACME",
    "execute": true
  }'
```

### Python으로 API 호출

```python
import requests

response = requests.post(
    'http://localhost:8000/api/query',
    json={
        'query': 'Create a Salesforce account named ACME',
        'execute': True
    }
)

result = response.json()
print(result['generated_code']['code'])
```

## 🎨 UI 커스터마이징

Web UI의 스타일을 변경하려면 [web_ui.py](web_ui.py)의 `<style>` 섹션을 수정하세요.

**예: 색상 테마 변경**
```css
body {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    /* 원하는 색상으로 변경 */
}
```

## 🔒 보안 고려사항

- **로컬 사용**: 기본적으로 `localhost`에서만 접근 가능
- **프로덕션**: 실제 서비스로 배포 시 인증/권한 추가 필요
- **API 키**: `.env` 파일을 공유하지 마세요

## 🐛 문제 해결

### 서버가 시작되지 않음
```bash
# 의존성 재설치
uv sync

# 포트 8000이 사용 중인지 확인
netstat -an | findstr 8000  # Windows
lsof -i :8000               # Linux/Mac
```

### API 키 오류
```bash
# .env 파일 확인
cat .env

# API 키가 설정되어 있는지 확인
ANTHROPIC_API_KEY=sk-ant-...
```

### MCP 구조 없음
```bash
# MCP 구조 먼저 생성
python main.py generate
```

## 📱 모바일 접근

같은 네트워크의 모바일에서 접근하려면:

1. 서버의 IP 주소 확인:
   ```bash
   ipconfig          # Windows
   ifconfig          # Linux/Mac
   ```

2. 모바일 브라우저에서 접속:
   ```
   http://[서버IP]:8000
   ```

## 🚀 성능 팁

- **대화형 모드**: 여러 질문을 연속으로 처리할 때 효율적
- **캐싱**: 같은 질문은 결과를 캐싱하여 재사용 (향후 기능)
- **병렬 처리**: 여러 탭에서 동시에 질문 가능

## 📚 추가 리소스

- [README.md](README.md) - 전체 프로젝트 개요
- [QUICKSTART.md](QUICKSTART.md) - CLI 사용 가이드
- [Anthropic MCP Docs](https://www.anthropic.com/engineering/code-execution-with-mcp)

---

**즐거운 코딩 되세요! 🎉**
